# OJ在线判题系统 JMeter 压力测试使用说明

## 文件说明
- `OJ_Stress_Test.jmx` - JMeter测试计划文件
- `JMeter使用说明.md` - 本使用说明文档

## 测试计划概述

本测试计划专门针对OJ系统的题目提交接口进行并发压力测试，解决了之前单独测试能入库但并发测试不入库的问题。

### 主要特性
- ✅ 自动处理用户登录和会话管理
- ✅ 支持并发题目提交测试
- ✅ 完整的断言验证（HTTP状态码 + 业务码）
- ✅ 详细的结果监听和报告
- ✅ 可配置的测试参数

## 使用前准备

### 1. 确保后端服务运行
```bash
# 确保OJ后端服务在 http://localhost:8101 运行
# 数据库连接正常，Redis服务启动
```

### 2. 准备测试数据
在使用测试计划前，请确保：
- 数据库中存在测试用户（默认：`test` / `12345678`）
- 数据库中存在可提交的题目（默认题目ID：`1`）

### 3. 修改测试参数（可选）
打开 `OJ_Stress_Test.jmx`，在"用户定义的变量"中可修改：
- `SERVER_HOST`: 服务器地址（默认：localhost）
- `SERVER_PORT`: 服务器端口（默认：8101）
- `BASE_PATH`: API基础路径（默认：/api）
- `USER_ACCOUNT`: 测试用户账号（默认：test）
- `USER_PASSWORD`: 测试用户密码（默认：12345678）
- `QUESTION_ID`: 测试题目ID（默认：1）

## 导入和运行测试

### 1. 导入测试计划
1. 打开 JMeter
2. 点击 `文件` → `打开`
3. 选择 `OJ_Stress_Test.jmx` 文件

### 2. 调整并发参数
在"OJ题目提交压力测试线程组"中可调整：
- **线程数（用户数）**: 默认50个并发用户
- **Ramp-Up时间**: 默认10秒内启动所有线程
- **循环次数**: 默认每个用户执行1次

### 3. 运行测试
1. 点击绿色的"启动"按钮
2. 观察"察看结果树"中的请求响应
3. 查看"聚合报告"和"汇总报告"了解性能指标

## 测试流程说明

### 执行步骤
1. **用户登录** (`POST /api/user/login`)
   - 使用配置的用户账号密码登录
   - 自动保存登录会话Cookie
   - 验证登录成功（业务码=0）

2. **等待间隔** (100ms)
   - 模拟真实用户操作间隔

3. **题目提交** (`POST /api/question/question_submit/do`)
   - 提交Java代码到指定题目
   - 携带登录会话进行认证
   - 验证提交成功（业务码=0）

### 关键组件说明

#### HTTP配置组件
- **HTTP请求默认值**: 统一配置服务器地址和超时时间
- **HTTP信息头管理器**: 设置Content-Type和Accept头
- **HTTP Cookie管理器**: 自动管理登录会话Cookie

#### 断言验证
- **HTTP状态码断言**: 确保请求返回200状态码
- **JSON业务码断言**: 确保业务逻辑成功（code=0）
- **BeanShell断言**: 详细的成功/失败日志记录

#### 结果监听器
- **察看结果树**: 查看每个请求的详细信息
- **聚合报告**: 统计性能指标（平均响应时间、吞吐量等）
- **汇总报告**: 简化的性能统计

## 常见问题排查

### 1. 登录失败
**现象**: 登录请求返回业务码非0
**解决方案**:
- 检查用户账号密码是否正确
- 确认用户在数据库中存在且状态正常
- 检查后端服务是否正常运行

### 2. 提交失败 - 未登录错误
**现象**: 提交请求返回业务码40100
**解决方案**:
- 确保Cookie管理器已启用
- 检查登录请求是否成功
- 确认Redis服务正常运行（用于session存储）

### 3. 提交失败 - 题目不存在
**现象**: 提交请求返回题目相关错误
**解决方案**:
- 检查QUESTION_ID参数是否指向存在的题目
- 确认题目状态为可提交状态

### 4. 数据库无记录
**现象**: 测试显示成功但数据库无新增记录
**解决方案**:
- 检查数据库连接配置
- 确认查看的是正确的数据库（my_db_oj）
- 查看后端日志是否有异常

## 性能指标参考

### 正常指标范围
- **平均响应时间**: 
  - 登录: < 500ms
  - 提交: < 1000ms
- **错误率**: < 1%
- **吞吐量**: 根据服务器配置而定

### 监控要点
- 数据库连接池使用情况
- Redis连接状态
- 服务器CPU和内存使用率
- 网络延迟

## 扩展测试场景

### 1. 增加并发用户数
逐步增加线程数，观察系统性能拐点：
- 50用户 → 100用户 → 200用户 → 500用户

### 2. 长时间压测
修改循环次数或使用调度器进行长时间测试：
- 设置循环次数为10或更多
- 或启用调度器运行5-10分钟

### 3. 不同代码长度测试
修改提交的代码内容，测试不同代码长度的性能影响。

### 4. 混合场景测试
结合其他接口（如题目列表查询）进行混合场景压测。

## 注意事项

1. **测试环境隔离**: 建议在测试环境而非生产环境运行
2. **数据清理**: 测试后及时清理测试数据
3. **资源监控**: 测试过程中监控服务器资源使用情况
4. **逐步加压**: 从小并发开始，逐步增加压力
5. **结果分析**: 重点关注错误率和响应时间趋势

## 技术支持

如遇到问题，请检查：
1. JMeter版本兼容性（推荐5.4+）
2. Java版本（推荐JDK8+）
3. 后端服务日志
4. 数据库和Redis连接状态

---
*测试计划版本: 1.0*  
*适用于: OJ在线判题系统*  
*更新时间: 2024年*